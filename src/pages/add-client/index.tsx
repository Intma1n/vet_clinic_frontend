import { Outlet } from 'react-router-dom';
import AddClientForm from './AddClientForm';

const AddClient = () => {
  return (
    <>
      <AddClientForm />
    </>
  );
};

export default AddClient;
