import { MenuItem, styled, Typography } from '@mui/material';

export const StyledLinksMenuItem = styled(MenuItem)`
  a {
    text-decoration: none;
    color: black;
    :active {
      color: black;
    }
    :hover {
      color: black;
    }
  }
`;
